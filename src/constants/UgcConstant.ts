export enum UgcType {
  ARTICLE = 'ARTICLE',
  POST = 'POST',
  QUESTION = 'QUESTION',
}

export enum InteractType {
  LIKE = 'LIKE',
  COLLECT = 'COLLECT',
  COMMENT = 'COMMENT',
  ADOPT = 'ADOPT',
}
